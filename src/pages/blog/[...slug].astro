---
const { slug } = Astro.params;
const normalizedSlug = Array.isArray(slug) ? slug.join("/") : slug;
const search = Astro.url.search;

if (!normalizedSlug) {
    return Astro.redirect(`/blog/vi${search}`, 301);
}

return Astro.redirect(`/blog/vi/${normalizedSlug}${search}`, 301);
---
